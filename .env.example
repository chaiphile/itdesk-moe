POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me
POSTGRES_DB=ticketing_db
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# Notes:
# - Copy this file to .env and set a secure POSTGRES_PASSWORD for local development.
# - Do NOT commit your real secrets.


# MinIO / S3-compatible storage (dev defaults)
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=a98319831a
MINIO_BUCKET=ticketing-attachments
S3_ENDPOINT=http://minio:9000
S3_ACCESS_KEY=${MINIO_ROOT_USER}
S3_SECRET_KEY=${MINIO_ROOT_PASSWORD}
S3_REGION=us-east-1
S3_SECURE=false
S3_PUBLIC_BASE_URL=http://localhost:9000

# Attachments settings
ATTACHMENTS_MAX_SIZE_BYTES=26214400
ATTACHMENTS_PRESIGN_EXPIRES_SECONDS=900
# Attachment retention and cleanup settings
# Default retention period for attachments after ticket closure (in days)
ATTACHMENT_RETENTION_DAYS=30
# How often to run retention cleanup job (in seconds, default 24 hours)
RETENTION_CLEANUP_INTERVAL=86400